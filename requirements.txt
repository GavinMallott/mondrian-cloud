Flask==2.2.2
Flask-PyMongo==2.2.0
PyMongo==4.3.3
Flask-Session==0.5.0  # Optional, if using MongoDB for session storage
Flask-OAuthlib==0.9.6  # Note: This version might need adjustments due to the error you faced
Werkzeug==2.2.2  # Pin this if you need the older url_* functionsi